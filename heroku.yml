setup:
  addons:
  - plan: heroku-postgresql
    as: DATABASE
build:
  docker:
    web: Dockerfile.prod
release:
  image: web
  command:
    - docker-compose -f docker-compose.prod.yml exec app python app/manage.py migrate --noinput
