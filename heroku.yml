setup:
  addons:
  - plan: heroku-postgresql
    as: DATABASE
build:
  docker:
    web: Dockerfile.prod
release:
  image: web
  command:
    - python app/manage.py migrate --noinput
run:
  web: gunicorn app/app.wsgi:application
